# These dependencies are intentionally on current releases. Some pnpm versions
# enforce a minimum-release-age supply-chain policy that would reject the frozen
# lockfile in a clean container build; disable it so installs are reproducible.
minimum-release-age=0
